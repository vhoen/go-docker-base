version: "3.5"
services:
  app:
    # build:
    #   context: .
    #   dockerfile: Dockerfile
    image: vhoen/go-base:1.2
    container_name: vhoen-go-base
    volumes:
      - .:/go/src/go-onsite-app
    ports:
      - "8000:8000"
